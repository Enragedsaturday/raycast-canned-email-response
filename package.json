{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "canned-replies",
  "title": "Canned Replies",
  "description": "Manage a list of canned email replies and quickly insert them in Apple Mail.",
  "version": "1.0.0",
  "author": "enragedsaturday",
  "license": "MIT",
  "icon": "icon.png",
  "categories": [
    "Productivity",
    "Applications"
  ],
  "platforms": ["macOS"],
  "keywords": [
    "apple mail",
    "email",
    "templates",
    "canned replies",
    "canned responses"
  ],
  "commands": [
    {
      "name": "index",
      "title": "Canned Replies",
      "description": "Search and insert saved email replies",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.102.0",
    "@raycast/utils": "^2.2.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "^18.0.0",
    "@types/react": "^17.0.0",
    "eslint": "^8.57.1",
    "typescript": "^4.5.4"
  },
  "scripts": {
    "dev": "npx ray develop",
    "build": "npx ray build",
    "lint": "npx ray lint",
    "type-check": "tsc --noEmit",
    "publish": "npx @raycast/api@latest publish",
    "prepublishOnly": "echo '\nThis project is a Raycast extension, not an npm package. Use `npm run publish` to submit to the Raycast Store.\n' && exit 1"
  }
}
